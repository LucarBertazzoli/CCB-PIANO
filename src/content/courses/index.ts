import type { Course } from '../types';

import { trilha } from './trilha';

/** Hoje há uma única trilha (baseada no MOR). Outras podem ser adicionadas aqui. */
export const courses: Course[] = [trilha];
